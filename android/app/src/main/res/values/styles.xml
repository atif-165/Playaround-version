<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- PlayAround App Colors -->
    <color name="playaround_primary">#247CFF</color>
    <color name="playaround_surface">#FEFBFF</color>
    <color name="playaround_background">#FEFBFF</color>
    <!-- PlayAround App Dark Colors (fallback for night mode) -->
    <color name="playaround_primary_dark">#ADC6FF</color>
    <color name="playaround_surface_dark">#10131A</color>
    <color name="playaround_background_dark">#10131A</color>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:statusBarColor">@color/playaround_primary</item>
        <item name="android:navigationBarColor">@color/playaround_surface</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/playaround_background</item>
        <item name="android:statusBarColor">@color/playaround_surface</item>
        <item name="android:navigationBarColor">@color/playaround_surface</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
