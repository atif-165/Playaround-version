import 'package:flutter_test/flutter_test.dart';

// TODO: Rebuild profile data service tests with Mocktail once the new Firebase
// mocking strategy lands. These placeholder assertions keep the analyzer clean
// while the migration is in progress.
void main() {
  test('profile data service tests temporarily disabled', () {
    expect(true, isTrue);
  });
}
