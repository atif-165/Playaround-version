import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'chat integration temporarily disabled pending Firebase test harness',
    () => expect(true, isTrue),
    skip: true,
  );
}
