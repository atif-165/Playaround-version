import 'skill_log.dart';

/// Alias that exposes [SkillLog] using the updated domain terminology.
typedef SessionLog = SkillLog;

/// Alias that exposes [SkillLogSource] as [SessionLogSource] for clarity.
typedef SessionLogSource = SkillLogSource;
